#!/usr/bin/env zsh
# Thanks ThePrimeagen

if [[ $# -eq 1 ]]; then
    selected=$1
else
    selected=$(git branch --list | fzf)
fi

if [[ -z $selected ]]; then
    exit 0
fi

echo $selected
echo git checkout $selected
git checkout $selected
