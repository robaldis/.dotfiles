#!/usr/bin/env zsh
# Thanks ThePrimeagen

echo "fjdklsa"
if [[ $# -eq 1 ]]; then
    selected=$1
else
    selected=$(git branch --list | fzf)
fi

if [[ -z $selected ]]; then
    exit 0
fi

git checkout $selected
